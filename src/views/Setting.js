import { View, Text, StatusBar } from "react-native"

export const Setting = ({navigation}) => {
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Pantalla de Setting de la Aplicación</Text>
      <StatusBar style="auto" />
    </View>
}